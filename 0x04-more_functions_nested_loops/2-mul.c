#include "main.h"
/**
 * mul - multiples two intergers
 * @x: the first number to multiply 
 * @y: the secont number to multiply with the fist number
 * return: the result of multiplication x param and y param
 */
int mul(int x, int y)
{
	return (x * y);
}
