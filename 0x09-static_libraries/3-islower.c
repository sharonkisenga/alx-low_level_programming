#include "main.h"
/**
 * _islower - checks for lowercase character
 * @c: the character to checker
 * return: 1 if c is lowercase, o otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
